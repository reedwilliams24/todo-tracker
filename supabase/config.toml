# Minimal Supabase CLI config; `supabase link --project-ref <ref>` before `pnpm db:migrate`.
project_id = "todo-tracker"

[auth]
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000", "todotracker://auth"]
